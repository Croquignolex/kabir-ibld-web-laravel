<div class="form-group">
    <div class="input-group form-check">
        <input id="{{ $id }}"
               type="checkbox"
               value="checked"
               name="{{ $id }}"
               {{ $attribute }}
               class="form-check-input"
        >
        <label for="copy">{{ $name }}</label>
    </div>
</div>
